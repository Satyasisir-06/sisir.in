#include<stdio.h>
#include<stdlib.h>
structnode
{
    int data;
    structnode*next;
};
structnode*newnode=(structnode*)malloc(sizeof(structnode));
newnode->data=data
newnode->next=newnode;
if(head==NULL){
    return newnode;
}
structnode*